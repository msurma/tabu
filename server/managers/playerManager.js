class PlayerManager {
    /**
     * Returns random player from room, other than specified in ignorePlayers parameter
     * @param {Room} room
     * @param {Player[]} ignorePlayers
     * @return {Player}
     */
    static getRandomPlayer(room, ignorePlayers) {

    }
}

module.exports = PlayerManager;